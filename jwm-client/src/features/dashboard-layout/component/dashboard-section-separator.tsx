import * as React from 'react';
import { Divider } from '@mui/material';

const DashboardSectionSeparator: React.FC = (): React.ReactElement => <Divider sx={{ my: 3 }} />;

export { DashboardSectionSeparator };
